## Nix configuration files

In theory, this will be used to setup a development environment for a new machine.

In practise, it'll probably break everything on your system. **Do not use unless you know what you are doing**

### Nothing works, everything is broken, this is less stable than an alpha build of Windows

Apply with: `home-manager switch --flake ~/.config/nix-config#<user>@<hostname>`

#### Useful links

- Home manager options: https://home-manager-options.extranix.com/
